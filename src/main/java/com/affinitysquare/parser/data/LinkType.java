package com.affinitysquare.parser.data;

/**
 * Represents the type of link in the system.
 *
 * @author Duclair Fopa
 * @version 1.0
 */
public enum LinkType {
    /**
     * Represents an internal link within the system.
     */
    INTERNAL,

    /**
     * Represents an external link that points to another system or website.
     */
    EXTERNAL;
}
